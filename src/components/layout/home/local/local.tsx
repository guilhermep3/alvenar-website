import { containerStyle } from "@/utils/container-style"

export const Local = () => {

   return (
      <section id="local">
         <div className={containerStyle+' flex'}>
            
         </div>
      </section>
   )
}